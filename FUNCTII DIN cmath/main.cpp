#include <iostream>
#include <cmath>
using namespace std;

int main() {
    /*
    int a = abs(2 - 3);
    double b = fabs(2.3 - 3.2);
    cout << a << " " << b;
    */
      int x = sqrt(167);
      cout << x;

     /*
        cout << floor(1.3) << " " << ceil(1.3) << " " << floor(-1.3) << " " << ceil(-1.3);

        /*
    int a = abs(2 - 3);
    double b = fabs(2.3 - 3.2);
    cout << a << " " << b;
    */
    return 0;
}
